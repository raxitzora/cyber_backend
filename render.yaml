services:
  - type: web
    name: cyber-backend
    runtime: python
    buildCommand: ""
    startCommand: uvicorn cyber:app --host 0.0.0.0 --port 8000
    envVars:
      - key: GROQ_API_KEY
        sync: false
